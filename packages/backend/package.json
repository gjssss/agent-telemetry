{
  "name": "@agent-telemetry/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "@agent-telemetry/core": "workspace:*",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
