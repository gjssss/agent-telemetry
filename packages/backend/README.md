# @auto-code/backend

Hono API server for the auto-code monorepo.

## Scripts

- Dev: `pnpm --filter ./packages/backend dev`
- Build: `pnpm --filter ./packages/backend build`
- Start: `pnpm --filter ./packages/backend start`
